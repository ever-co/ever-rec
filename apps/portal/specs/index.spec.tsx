import React from 'react';

describe('Index', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<MediaIndex />);
    // expect(baseElement).toBeTruthy();
  });
});
