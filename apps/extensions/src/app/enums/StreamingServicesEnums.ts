export enum VideoServicesEnum {
  MUX = 'mux',
  API_VIDEO = 'api.video',
  CLOUDINARY = 'cloudinary',
  LOCAL_STORAGE = 'local storage',
}

export enum PlaybackStatusEnum {
  PREPARING = 'preparing',
  READY = 'ready',
  ERRORED = 'errored',
}
