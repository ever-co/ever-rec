/** Supported pipelines  */
export enum PipelineType {
  UPLOAD_IMAGE = 'upload → image',
  UPLOAD_VIDEO = 'upload → video',
  UPLOAD_SOUNDSHOT = 'upload → audio',
  UPLOAD_CAMSHOT = 'upload → photo',
}
